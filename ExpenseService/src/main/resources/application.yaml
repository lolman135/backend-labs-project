spring:
  application:
    name: ExpenseService

  datasource:
    url: ${EXPENSE_DB_URL:jdbc:postgresql://dpg-d450msvgi27c73aksnj0-a.frankfurt-postgres.render.com:5432/expense-db?sslmode=require}
    username: ${EXPENSE_DB_USERNAME:main_user}
    password: ${EXPENSE_DB_PASSWORD:nhxkff1DGkNVDWtFhRGTsEAQnOk8ssNk}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8083

user-service:
  url: http://localhost:8082

currency-service:
  url: http://localhost:8084