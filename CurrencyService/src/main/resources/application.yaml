spring:
  application:
    name: CurrencyService

  datasource:
    url: ${CURRENCY_DB_URL:jdbc:postgresql://dpg-d450msvgi27c73aksnj0-a.frankfurt-postgres.render.com:5432/currency-db?sslmode=require}
    username: ${CURRENCY_DB_USERNAME:main_user}
    password: ${CURRENCY_DB_PASSWORD:nhxkff1DGkNVDWtFhRGTsEAQnOk8ssNk}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8084