spring:
  application:
    name: UserService

  docker:
    compose:
      enabled: false

  datasource:
    url: ${USER_DB_URL:jdbc:postgresql://dpg-d450msvgi27c73aksnj0-a.frankfurt-postgres.render.com:5432/user-db?sslmode=require}
    username: ${USER_DB_USERNAME:main_user}
    password: ${USER_DB_PASSWORD:nhxkff1DGkNVDWtFhRGTsEAQnOk8ssNk}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082

currency-service:
  url: http://localhost:8084